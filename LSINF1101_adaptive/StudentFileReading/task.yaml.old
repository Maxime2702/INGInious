accessible: false
author: Tanguy De Bels
categories: []
context: |-
    Dans cet exercice, il vous est demandé de créer une fonction créant des instances de ``Student`` basées sur les informations contenus dans un fichier et les renvoyant dans une liste. Ceci afin de récupérer les notes de l'interros des différents participants.

    Voici le constructeur de la classe ``Student``:

    .. code-block:: python

        class Student:
            def __init__(self, fname, sname, mark):
                self.fname = fname
                self.sname = sname
                self.mark = mark

    Le fichier suit le format suivant:

    .. code-block:: python

        Kim Mens 12
        Charles Pecheur 13
        Siegfried Nijssen 18



    .. raw:: html

       <script defer>

       bodyHeight = 0;
       function postSize() {
           if (document.body.scrollHeight != bodyHeight) {
               bodyHeight = document.body.scrollHeight;
               window.parent.postMessage({height: document.body.scrollHeight}, "*");
           }
       };
       var target = document.querySelector('body');

       var observer = new MutationObserver(postSize);

       var config = { attributes: true, subtree: true }

       observer.observe(target, config);
       $(document).ready(function(){
           setTimeout(postSize, 0);
       });
       </script>
environment: default
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    time: '30'
    memory: '100'
    output: '2'
name: 'Session 9: Q* Création d''objets depuis des fichiers'
network_grading: false
order: 109
problems:
    q1:
        type: code
        name: Implémentation
        language: python
        header: Implémentez la fonction ``marks_from_file(filename)`` en Python.
        default: ''
run_cmd: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
