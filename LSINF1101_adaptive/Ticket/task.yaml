accessible: true
author: Tanguy De Bels, Kim Mens
categories:
- Variable_instance
contact_url: ''
context: |-
    Complétez la classe ``Ticket`` ci-dessous:

    .. code-block:: python

        """
        Un ticket de parking
        """
        class Ticket :

            __prochain_numero = 1  # variable de classe pour générer le numéro du ticket

            def __init__(self) :
                """
                @pre  -
                @post Crée un ticket avec un nouveau numéro.
                      Les numéros sont attribués séquentiellement à partir de 1.
                """
                    # A COMPLETER

            def numero(self):
                """
                @pre  -
                @post retourne le numero de billet
                """
                return self.__numero

    .. raw:: html

       <script defer>

       bodyHeight = 0;
       function postSize() {
           if (document.body.scrollHeight != bodyHeight) {
               bodyHeight = document.body.scrollHeight;
               window.parent.postMessage({height: document.body.scrollHeight}, "*");
           }
       };
       var target = document.querySelector('body');

       var observer = new MutationObserver(postSize);

       var config = { attributes: true, subtree: true }

       observer.observe(target, config);
       $(document).ready(function(){
           setTimeout(postSize, 0);
       });
       </script>
environment: default
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    time: '30'
    output: '2'
    memory: '100'
name: 'Session 9: Ticket - Variable de classe'
network_grading: false
order: 119
problems:
    q1:
        header: Ajoutez les méthodes de la classe ``Ticket`` que vous considérez nécessaire
            et implémentez la fonction ``__init__`` de la clase ``Ticket`` en Python.
        name: Implémentation
        language: python
        type: code
        default: ''
run_cmd: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
