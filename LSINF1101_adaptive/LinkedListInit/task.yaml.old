accessible: false
author: Tanguy De Bels
categories: []
context: |-
    Changez la méthode d'initialisation ``__init__`` de la classe ``LinkedList``, afin qu'elle prenne une liste Python comme paramètre pour initialiser la liste chaînée. Spécifier bien cette nouvelle méthode ``__init__`` avec des conditions pre et post.

     .. code-block:: python

        def __init__(self, lst=[]):
            self.__length = 0
            self.__head = None
            # initialiser la liste chainêé avec les éléments de lst ...

    Par exemple:

    .. code-block:: python

        >>> l = LinkedList([1,2,3])
        >>> l.print()
        [ 1 2 3 ]

        >>> l = LinkedList([])
        >>> l.print()
        [ ]

    .. raw:: html

       <script defer>

       bodyHeight = 0;
       function postSize() {
           if (document.body.scrollHeight != bodyHeight) {
               bodyHeight = document.body.scrollHeight;
               window.parent.postMessage({height: document.body.scrollHeight}, "*");
           }
       };
       var target = document.querySelector('body');

       var observer = new MutationObserver(postSize);

       var config = { attributes: true, subtree: true }

       observer.observe(target, config);
       $(document).ready(function(){
           setTimeout(postSize, 0);
       });
       </script>
environment: default
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    memory: '100'
    time: '30'
    output: '2'
name: 'Session 11: LinkedList - __init__'
network_grading: false
order: 123
problems:
    q1:
        default: ''
        language: python
        name: Implémentation
        type: code
        header: Implémentez la fonction ``__init__`` de la classe ``LinkedList`` en
            Python.
run_cmd: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
