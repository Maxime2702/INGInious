accessible: true
author: Tanguy De Bels
categories:
- Objet
contact_url: ''
context: |-
    Une liste de coordonnées est donnée. Par exemple:

    .. code-block:: python

        l = [(2.0, 5.0), (8.0, 12.0), (10.0, 40.0), (8.0, 50.0), (8.0, 50.0)]

    Nous voulons créer un dictionnaire pour identifier rapidement
    les valeurs ``y`` pour un ``x`` donné. Par exemple:

    .. code-block:: python

        d = {2.0: [5.0], 8.0: [12.0, 50.0, 50.0], 10.0: [40.0]}


    Écrivez une fonction ``create_dict(l)`` qui crée ce dictionnaire (pas limité à cet exemple).

    .. raw:: html

       <script defer>

       bodyHeight = 0;
       function postSize() {
           if (document.body.scrollHeight != bodyHeight) {
               bodyHeight = document.body.scrollHeight;
               window.parent.postMessage({height: document.body.scrollHeight}, "*");
           }
       };
       var target = document.querySelector('body');

       var observer = new MutationObserver(postSize);

       var config = { attributes: true, subtree: true }

       observer.observe(target, config);
       $(document).ready(function(){
           setTimeout(postSize, 0);
       });
       </script>
environment: default
evaluate: last
file: ''
groups: false
input_random: '0'
limits:
    time: '30'
    output: '2'
    memory: '100'
name: 'Session 7: Création de dictionnaire'
network_grading: false
order: 75
problems:
    q1:
        language: python
        name: Implémentation
        type: code
        header: Implémentez la fonction ``create_dict(l)`` en Python.
        default: ''
run_cmd: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
