accessible: false
author: Tanguy De Bels
categories: []
contact_url: ''
context: |-
    Données sont:

    * une matrice representée en utilisant des listes imbriquées

    * une matrice representée en utilisant un dictionnaire, où les zéros ne sont pas stockées.

    *Par exemple:*

    .. code-block:: python

        l = [[0, 2, 4], [4, 1, 0]]
        d = {(0,1): 2, (0,2): 4, (1,0): 4, (1,1): 1}

    Écrivez une fonction ``equal(l,d)`` qui détermine si ``d`` contient les même valeurs que ``l`` pour chaque entrée de ``l``. (Nous permettons que ``d`` soit plus large que ``l``.)

    .. raw:: html

       <script defer>

       bodyHeight = 0;
       function postSize() {
           if (document.body.scrollHeight != bodyHeight) {
               bodyHeight = document.body.scrollHeight;
               window.parent.postMessage({height: document.body.scrollHeight}, "*");
           }
       };
       var target = document.querySelector('body');

       var observer = new MutationObserver(postSize);

       var config = { attributes: true, subtree: true }

       observer.observe(target, config);
       $(document).ready(function(){
           setTimeout(postSize, 0);
       });
       </script>
environment: default
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    memory: '100'
    output: '2'
    time: '30'
name: 'Session 7: Egalité entre structures'
network_grading: false
order: 74
problems:
    q1:
        name: Implémentation
        default: ''
        type: code
        header: Implémentez la fonction ``equal(l, d)`` en Python.
        language: python
run_cmd: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
