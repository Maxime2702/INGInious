accessible: true
author: Tanguy De Bels
categories:
- Array_List
contact_url: ''
context: |-
    Soit une structure de données comparable à la suivante:

    .. code-block:: python

        l = [{"City": "Bruxelles", "Country": "Belgium"}, \
            {"City": "Berlin", "Country": "Germany"}, \
            {"City": "Paris", "Country": "France"}]

    Écrivez une fonction ``get_country(l,name)`` qui, pour le nom d'une ville ``name`` et une structure de données ``l``, retourne le nom du pays dans lequel la ville est localisée. La fonction retourne ``False`` si la ville n'est pas dans ``l``.

    .. raw:: html

       <script defer>

       bodyHeight = 0;
       function postSize() {
           if (document.body.scrollHeight != bodyHeight) {
               bodyHeight = document.body.scrollHeight;
               window.parent.postMessage({height: document.body.scrollHeight}, "*");
           }
       };
       var target = document.querySelector('body');

       var observer = new MutationObserver(postSize);

       var config = { attributes: true, subtree: true }

       observer.observe(target, config);
       $(document).ready(function(){
           setTimeout(postSize, 0);
       });
       </script>
environment: default
evaluate: best
file: ''
groups: false
input_random: '0'
limits:
    memory: '100'
    output: '2'
    time: '30'
name: 'Session 7: Utilisation des clés'
network_grading: false
order: 78
problems:
    q1:
        name: Implémentation
        default: ''
        type: code
        header: Implémentez la fonction ``get_country(l, name)`` en Python.
        language: python
run_cmd: ''
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
